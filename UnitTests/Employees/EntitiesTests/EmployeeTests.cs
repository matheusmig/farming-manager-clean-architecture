using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace UnitTests.Employees.EntitiesTests
{
    public class EmployeeTests
    {
        [Fact]
        public void Employee_Should_Be_Registered_With_1_Account()
        {
            /*var entityFactory = new Infrastructure.InMemoryDataAccess.EntityFactory();

            // Arrange
            IEmployee employee = entityFactory.NewEmployee(
                new PISPASEP("123918231211"),
                new Name("Ivan Paulovich"));

            var account = entityFactory.NewAccount(sut.Id);

            // Act
            sut.Register(account.Id);

            // Assert
            Assert.Single(sut.Accounts.GetAccountIds());*/
        }
    }
}
